--- Error 1: "the if condition must be a scalar value" ---
void test1() { // at.c decomentat
    if (a) {
        a.n = 1;
    }
}

--- Error 2: "the while condition must be a scalar value" ---
void test2() { // at.c decomentat
    while (a) {
        a.n = 1;
    }
}

--- Error 3: "a void function cannot return a value" ---
void test3() {
    f(v.text, 0, '#');
    return 1;
}

--- Error 4: "the return value must be a scalar value" ---

int test4(int x, int y) { // at.c decomentat
    return a;
}

--- Error 5: "cannot convert the return expression type to the function return type" ---
int test5(int x, int y) { // at.c comentat
    return "text";
}

--- Errro 6: "a non-void function must return a value" ---
int test6(int x, int y) {
    return;
}

--- Error 7: "the assign destination must be a left-value" ---
void test7() {
    5 = a.n;
}

--- Error 8: "the assign destination cannot be constant" ---
void test8() {
    a.text = "salut";
}

--- Error 9: "the assign destination must be scalar" ---
void test9() {
    a = 5; // decometat at.c
}

--- Error 10: "the assign source must be scalar" ---
void test10() { // decomentat at.c
    int x;
    x = a;
}

--- Error 11: "the assign source cannot be converted to destination" ---
struct S2 { int y; };
struct S2 b;

void test11() {
    a = b;
}

--- Error 12: "invalid operand type for ||" ---
void test12() {
    int x;
    x = v || 1;
}

--- Error 13: "invalid operand type for &&" ---
void test13() {
    int x;
    x = v && 1;
}

--- Error 14: "invalid operand type for ==" ---
void test14() {
    int x;
    x = v == 1;
}

--- Error 15: "invalid operand type for =!" ---
void test15() {
    int x;
    x = v != 1;
}

--- Error 16: "invalid operand type for <" ---
void test16() {
    int x;
    x = v < 1;
}

--- Error 17: "invalid operand type for <=" ---
void test17() {
    int x;
    x = v <= 1;
}

--- Error 18: "invalid operand type for >" ---
void test18() {
    int x;
    x = v > 1;
}

--- Error 19: "invalid operand type for >=" ---
void test19() {
    int x;
    x = v >= 1;
}

--- Error 20: "invalid operand type for +" ---
void test20() {
    int x;
    x = v + 1;
}

--- Error 21: "invalid operand type for -" ---
void test21() {
    int x;
    x = v - 1;
}

--- Error 22: "invalid operand type for *" ---
void test22() {
    int x;
    x = v * 1;
}

--- Error 23: "invalid operand type for /" ---
void test23() {
    int x;
    x = v / 1;
}

--- Error 24: "cannot convert to a struct type" ---
void test24() {
    int x;
    x = (struct S)x;
}

--- Error 25: "cannot convert a struct" ---
void test25() {
    int x;
    x = (int)a;
}

--- Error 26: "an array can be converted only to another array" ---
void test26() {
    int x;
	int w[20];
    x = (int)w;
}

--- Error 27: "a scalar can be converted only to another scalar" ---
void test27() {
    int x;
    x = (int[])x;
}

--- Error 28: "a field can only be selected from a struct" ---
void test28() {
    int x;
    int y;
    y = x.n;
}

--- Error 29: "the structure %s does not have a field %s" ---
void test29() {
    int x;
    x = a.xyz;
}

--- Error 30: "only an array can be indexed" ---
void test30() {
    int x;
    int y;
    y = x[0];
}

--- Error 31: "the index is not convertible to int" ---
void test31() {
    int x;
    x = v[a];
}

--- Error 32: "a function can only be called" ---
void test32() {
    int x;
    x = f + 1;
}

--- Error 33: "too few arguments in function call" ---
void test33() {
    f(v[0].text, 0);
}

--- Error 34: "in call, cannot convert the argument type to the parameter type" ---
void test34() {
    f(v[0].text, a, '#');
}

--- Error 35: "too many arguments in function call" ---
void test35() {
    f(v[0].text, 0, '#', 1);
}

--- Error 36: "only a function can be called" ---
void test36() {
    int x;
    int y;
    y = x();
}

--- Error 37: "undefined id: %s" ---
void test37() {
    int x;
    x = xyz;
}
